# TODO

- [ ] Convert `frontend/src/pages/Admin/CollectionSchedule.jsx` from mock data to live API-backed data.
- [ ] Fix redundancies: remove/wire unused `view` state and clean up any unused computations/constants.
- [ ] Add loading + error states to prevent blank/crashing UI.
- [ ] Derive weekly grid from fetched `deployment-schedules/` records (bot/day/status/label/zone).
- [ ] Smoke test by running backend + frontend and verifying the page renders without console errors.


